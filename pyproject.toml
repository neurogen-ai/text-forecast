[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
text-forecast = "main:app"

[project]
name = 'text-forecast'
description = 'Forecast outcomes from text corpora over time, with NLP'

version = '2.4.5'
requires-python = '>=3.13,<3.14'

readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]

dependencies = [
    'polars==1.38.1',
    'pandas==2.3.3',
    'mlflow==3.9.0',
    'transformers==4.48.0',
    'scikit-learn==1.8.0',
    'typer==0.23.1',
    'seaborn==0.13.2',
]

[project.optional-dependencies]
modal = ["modal>=0.73.0"]
cpu = ["torch>=2.14"]
cuda126 = ["torch>=2.14"]
cuda130 = ["torch>=2.14"]

[dependency-groups]
dev = ["pytest>=8.2"]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.uv]
conflicts = [
    [
        { extra = "cpu" },
        { extra = "cuda126" },
        { extra = "cuda130" },
    ],
]

[tool.uv.sources]
torch = [
    { index = "pytorch-cpu", extra = "cpu" },
    { index = "pytorch-cu126", extra = "cuda126" },
    { index = "pytorch-cu130", extra = "cuda130" },
]


[[tool.uv.index]]
name = "pytorch-rocm10"
url = "https://download.pytorch.org/whl/nightly/rocm10.0"
explicit = true

[[tool.uv.index]]
name = "pytorch-rocm714"
url = "https://download.pytorch.org/whl/rocm7.14"
explicit = true

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

authors = [
    { name = 'Felix Noble', email = 'felix.noble@live.co.uk' }
]
