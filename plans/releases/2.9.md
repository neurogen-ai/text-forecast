# Plan 2.8 — Remote progress tracking for cloud runs

> **NOTE: RECONSTRUCTED.** The original file was lost during a renaming
> mistake on 2026-06-05. The header, scope and future-steps sections below are
> recovered verbatim; everything between them has been rebuilt from cross-
> references in plans 2.6/2.7 and should be reviewed against your memory of
> the original before this plan is executed.
>
> **Audit flags (added 2026-08-26, from a consistency review):**
>
> 1. **Numbering gap.** The prose in the rebuilt middle section is
>    unnumbered and jumps straight to "## 5. Future steps", implying that
>    numbered sections 1–4 existed in the lost original and must be
>    reconstructed from memory before execution.
> 2. **Missing structure.** The rebuilt section lacks the branch/task
>    breakdown, verification section, and concrete module paths that
>    sibling plans carry; those likely lived in the lost original.
> 3. **F-numbering starts at F-34.** Decisions F-1 through F-33 are not
>    accounted for in this file; their provenance must be confirmed
>    before any numbered decision is relied on.
>
> **Scope:** give spawned (detached) Modal runs a live progress channel.
> Plan 2.0 §11 made `Engine` progress optional (`progress=None` in containers)
> so headless execution works; the cost is that a spawned 24h training run is
> invisible until metrics land. This plan replaces the `None` guards with a
> real reporting abstraction.
>
> **Prerequisites:** plan 2.0 is complete (headless Engine, spawned dispatch
> with `RunResult.modal_function_call_id`).
>
> This is a v2.x release: additive internals, no CLI or experiment contract
> changes.

---

<!-- Reconstructed middle section: goal, reporter abstraction, batching,
     MLflow-first channel. Verify before executing. -->

The engine currently takes `progress: ProgressBars | None`. In a spawned Modal
container that is `None`, so nothing reports until metrics land in MLflow.
This plan introduces a venue-independent reporter interface that the Engine
and tracker reporting paths call instead of checking for `None`:

- A minimal `Reporter` protocol with heartbeat/progress/metric hooks.
- A local implementation wrapping the existing rich progress bars.
- A remote implementation that batches updates and flushes them to MLflow
  (metrics first, per the owner's preference), so a detached run is followable
  via `mlflow` or `modal.FunctionCall.from_id` while it executes.

## 5. Future steps

- **F-34. Status endpoint / web surface.** If MLflow batching proves too slow
  for interactive following, promote the reporter to POST heartbeats to a
  lightweight endpoint (ties into plan 2.6 §8 web endpoints).
- **F-35. ETA/heartbeat-based hang detection.** Feed reporter heartbeats into
  the plan-2.6 §6.2 health checks to detect hung containers automatically.
